/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "tailwindcss.com",
      "www.svgrepo.com",
      "lh3.googleusercontent.com",
      "avatars.githubusercontent.com",
    ], // add your own domain here if needed,
  },
};

module.exports = nextConfig;
